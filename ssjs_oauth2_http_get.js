/*****************************************************************************
Authorization header setup in case the API requires authorization header token (Oauth2)
*****************************************************************************/

// Ideally I would have the token stored as an env variable somewhere for security
// var mytoken = "sometokenvalue"

// var headerNames = ["Accept", "Authorization"];
// var headerValues = ["application/json", "Bearer "+mytoken];

// var response = HTTP.Get(url, headerNames, headerValues);